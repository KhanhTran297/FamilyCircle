const PostsContent = (props) => {
  const { bg } = props;
  return (
    <div
      style={{
        padding: 24,
        minHeight: 360,
        background: bg,
      }}
    >
      PostsContent
    </div>
  );
};

export default PostsContent;
