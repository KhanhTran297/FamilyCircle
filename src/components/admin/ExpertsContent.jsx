const ExpertsContent = (props) => {
  const { bg } = props;
  return (
    <div
      style={{
        padding: 24,
        minHeight: 360,
        background: bg,
      }}
    >
      ExpertsContent
    </div>
  );
};

export default ExpertsContent;
