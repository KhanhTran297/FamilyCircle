const UsersContent = (props) => {
  const { bg } = props;
  return (
    <div
      style={{
        padding: 24,
        minHeight: 360,
        background: bg,
      }}
    >
      UsersContent
    </div>
  );
};

export default UsersContent;
